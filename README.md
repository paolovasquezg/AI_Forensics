# Data Visualization: Project 2
**VAST Challenge 2026 — Mini-Challenge 2**

---

## Environment setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

---

## Project structure

```
data/
  graph.json              — Org chart (company, departments, teams, persons)
  interactions.json       — 185 147 raw events (May 8 – Jul 16, 2046)
  transf/                 — Generated by transformations/transf.ipynb
    ai_events.json
    delegation_chains.json
    triggered_events.json
    agent_sessions.json
    daily_aggregates.json
    incident_chains.json
    anomaly_labels.json
    saidit_posts_annotated.json
    c2_beacons.json
    agent_propagation_metrics.json
    intervention_edges.json

exploration/              — Exploratory notebooks (read raw data, no writes)
  exp.ipynb               — Data Exploration
  eda.ipynb               — Exploratory Data Analysis
  vis.ipynb               — Interactive Visualizations

transformations/          — Data pipeline
  transf.ipynb            — Data Transformation Pipeline (generates data/transf/)

info/
  instrucciones.md        — Full project guide, data schema, and Observable notebook spec
```

---

## Workflow

### 1. Explore
Open the notebooks in `exploration/` to understand the raw data before running any transformations. No files are written.

| Notebook | Purpose |
|----------|---------|
| `exp.ipynb` | Org chart and general interaction network |
| `eda.ipynb` | AI vs. human activity breakdown, delegation patterns, temporal trends |
| `vis.ipynb` | Interactive charts reading from `data/transf/` |

### 2. Transform
Open `transformations/transf.ipynb` and run all cells in order (**Kernel → Restart & Run All**).

Produces 11 files in `data/transf/` — 5 base datasets (cells 1–5) followed by 6 incident-aware enrichments (cells 6–11) needed for the Observable forensic dashboard.

---
